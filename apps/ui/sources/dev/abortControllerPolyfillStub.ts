// Vitest/node stub for `abort-controller/polyfill.mjs`.
//
// The real ESM polyfill uses extensionless ESM imports (`./dist/abort-controller`) which Node cannot resolve.
// Node (>=16) already provides a built-in AbortController, so tests don't need this polyfill.

export {};

